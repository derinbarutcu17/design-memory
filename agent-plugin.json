{
  "name": "design_memory_audit",
  "description": "Executes a local staged-file design policy audit against the synced reference snapshot for the current repository.",
  "inputs": {
    "type": "object",
    "properties": {
      "cwd": {
        "type": "string",
        "description": "Absolute repository path to audit. Defaults to the current working directory."
      }
    },
    "required": [],
    "additionalProperties": false
  },
  "execution": "node dist/cli/index.js audit"
}
